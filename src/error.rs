/// Error type for Protofish2 operations.
///
/// Currently a placeholder for future error handling. Application-specific
/// errors are returned through other error types like `ProtofishConnectionError`.
#[derive(Debug)]
pub enum ProtofishError {}
